on: [push, pull_request]

name: Continuous integration Matrix

jobs:
  matrix:
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-24.04", "windows-2025"]
    uses: "./.github/workflows/ci_item.yml"
    with:
      os: ${{ matrix.os }}
    secrets: inherit
